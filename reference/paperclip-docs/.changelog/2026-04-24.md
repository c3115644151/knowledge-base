# 更新日志 - 2026-04-24

## 源仓库更新

**仓库**: paperclipai/paperclip
**分支**: master
**最新 Commit**: f98c348e2b3011b2fdc905c7a41f73d4f1998ac3
**日期**: 2026-04-23

## Commit 信息

### [codex] Add issue subtree pause, cancel, and restore controls (#4332)

**作者**: Dotta <bippadotta@protonmail.com>

**核心变更**:
- 新增 durable issue subtree hold 数据结构
- 新增共享 API/types/validators
- 新增 server routes/services
- 新增 UI flows 支持 subtree pause、cancel、restore 操作
- 新增 subtree previewing、hold 创建/释放
- 新增 dependency-aware scheduling under holds
- 新增 Issue detail 子树控制 UI
- 迁移文件 `0066_issue_tree_holds`（避免与上游 `0065_environments` 冲突）

**风险提示**:
- 广泛的跨层变更（DB/schema、shared contracts、server orchestration、UI）
- 可能回归：subtree status restoration 或 wake suppression/resume edge cases
- Restore endpoint 现在返回 `200 OK` 而非隐式行为

---

## 文档更新

### adapters/codex-local.md

**变更类型**: 更新
**变更内容**:
1. 新增「手动本地 CLI」章节 - 介绍如何使用 `local-cli` 命令手动运行 codexcoder
2. 新增「指令解析」章节 - 说明 `instructionsFilePath` 配置和 AGENTS.md 行为

**同步状态**: ✅ 已完成

---

## 新增文档（来自仓库）

本次 Commit 新增以下文档（本地知识库已有对应文件，本次确认无需额外同步）:

- docs/adapters/adapter-ui-parser.md - 适配器 UI 解析器合约
- docs/adapters/claude-local.md - Claude Local 适配器配置
- docs/adapters/codex-local.md - Codex Local 适配器配置（已同步）

---

## 验证命令

```bash
pnpm --filter @paperclipai/db typecheck
pnpm --filter @paperclipai/shared typecheck
pnpm --filter @paperclipai/server typecheck
pnpm --filter @paperclipai/ui typecheck
cd server && pnpm exec vitest run src/__tests__/issue-tree-control-routes.test.ts
cd server && pnpm exec vitest run src/__tests__/issue-tree-control-service.test.ts
cd server && pnpm exec vitest run src/__tests__/issue-tree-control-service-unit.test.ts
cd server && pnpm exec vitest run src/__tests__/heartbeat-dependency-scheduling.test.ts
cd ui && pnpm exec vitest run src/components/IssueChatThread.test.tsx
cd ui && pnpm exec vitest run src/pages/IssueDetail.test.tsx
```
