# 更新日志 2026-05-29

**4 个仓库有更新：**

---

## nousresearch/hermes-agent
**105 commits | v0.15.0 + v0.15.1 双版本发布**

### 🔄 重大功能

**v0.15.0 (2026.5.28)**
- **Web 重构**：消费 DS primitives，移除本地组件副本，添加可折叠侧边栏、移动端滚动修复
- **Skills Hub 扩展**：ClawHub 目录从 200 → 20k+，skills.sh 从 858 → 19,932
- **Docker persist-mode**：容器跨进程复用、orphan reaper、cleanup 泄漏修复
- **MCP mTLS 支持**：支持 TLS 客户端证书（mTLS）用于 HTTP/SSE 服务器
- **Auth 改进**：Codex pool sync、Nous entitlement refresh

**v0.15.1 (2026.5.29)**
- **Kanban 改进**：图片附件、default_assignee fallback、per-profile concurrency cap
- **Dashboard 修复**：/api/auth/me 401 不再触发重载循环
- **Docker 标签**：容器添加 hermes-agent labels，persist 模式修复

### 🔧 重要修复
- `fix(gateway)`: drain on Windows `hermes gateway stop` 会话重启保留
- `fix(agent)`: 立即 fallback on provider content-policy blocks
- `fix(xai-oauth)`: 接受 bare-code manual paste
- `fix(model picker)`: 统一 /model 和 `hermes model` 列表，添加磁盘缓存
- `fix(mcp)`: 解析 bare npx/npm/node against /usr/local/bin
- `fix(skills-page)`: 懒加载 catalog（避免 34MB bundle）
- `fix(kanban)`: SIGTERM on worker 终止进程
- `fix(redact)`: web URLs 透传不变
- `fix(stream-consumer)`: 只有确认最终响应才设置 _final_content_delivered
- `fix(agent)`: preload jiter native parser

### 🛡️ 安全修复
- `fix(security)`: require API_SERVER_KEY before dispatching API server work
- `fix(security)`: require source CIDR allowlisting for public msgraph webhook binds
- `fix(kanban)`: reject unsafe tar members (Android psutil 兼容)
- `fix(tirith)`: reject non-regular tar members during auto-install

### 📝 文档更新
- 30-day 文档大修：correctness audit、PR coverage、Nous Portal weave、sidebar reorg
- `docs(docker)`: 刷新 s6-overlay reality 用户指南
- `docs(email)`: clarify gateway vs Himalaya setup
- `docs(hindsight)`: correct recall_types scope
- `docs(reference)`: document --no-supervise / HERMES_GATEWAY_NO_SUPERVISE

### 📊 性能优化
- `perf(skills-page)`: 懒加载 catalog，避免 34MB JS bundle
- `fix(kanban)`: harden sqlite connection concurrency
- `fix(kanban)`: close kanban.db FD after every connect()

### 🔗 其他
- `feat(openrouter)`: pass session_id in extra_body for sticky routing
- `feat: add claude-opus-4.8 and claude-opus-4.8-fast`
- `feat(hindsight)`: default recall_types to observation only
- `feat(context-engine)`: host contract for external context engines
- `feat(image_gen/fal)`: add Krea 2 Medium + Large to FAL catalog

---

## paperclipai/paperclip
**2 commits**

- `[codex] Add private browser first-admin claim flow (#6755)`
- `[codex] Add skills CLI and catalog management (#6782)`

**🔧 功能更新** - Skills CLI 和私有浏览器 first-admin claim 流程

---

## FabricMC/fabric-docs
**3 commits**

- `Rendering in world fixes (#591)`
- `Remove magic numbers from DirtChestMenu (#614)`
- `Event page mapping fix (#595)`

**⚪ 维护** - 渲染修复、代码清理、事件页映射修复

---

## neoforged/documentation
**1 commit**

- `[21.1-26.1] Fix LootTableSubProvider#generate example (#355)`

**⚪ 维护** - 修复 LootTableSubProvider.generate 示例

---

## tukuaiai/vibe-coding-cn
无更新
