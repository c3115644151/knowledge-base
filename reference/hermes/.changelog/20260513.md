# Hermes Agent 更新日志

> 日期：2026-05-13

## 变更概述

- 约 54 个 commits
- **重要安全修复**：approval DELETE 模式绕过漏洞
- **消息平台全面改进**：Telegram/WhatsApp/WeChat/Line/WeCom
- **LSP 增强**：语义诊断功能

---

## 安全修复

### 🚨 approval DELETE 模式绕过 (Critical)
- **问题**：DOTALL 标志允许换行绕过
- **修复**：正则模式加固

### Supply Chain Advisory Checker
- 新增供应链安全检查器
- Lazy-install 框架
- 分层安装回退机制

---

## 消息平台改进

### Telegram
- 消息发送修复：`thread_id` 通过 metadata 转发
- 斜杠确认：`use thread fallback helper`
- 反应清除：取消处理时清理进行中状态
- 菜单命令：支持带参数命令
- Inline Keyboard：`clarify tool` 按钮集成

### WhatsApp
- npm install 超时可配置
- `$HOME` 目标环境变量包含 whatsapp

### WeCom
- WebSocket 重连后更新连接状态

### Line
- 修复 `create_source` → `build_source`

### WeChat
- Gateway 帮助和文档字符串新增 WeChat 提及

---

## LSP 增强

### 语义诊断 (Semantic Diagnostics)
- **PR #24168**：集成真实语言服务器的语义诊断
- 支持 `write_file` 和 `patch` 工具
- TypeScript SDK 安装修复
- `tsc-missing` 跳过逻辑

### 文档更新
- LSP follow-up fixes 文档

---

## Gateway 改进

- `chat_id` 加入 hook_ctx 用于消息源追踪
- systemd 重启延迟优化
- Clarify tool 内联键盘按钮（Telegram）

---

## 其他修复

### Voice Mode
- WSL 音频检测：`PULSE_SERVER` 环境下 `sd.query_devices()` 返回空列表处理

### Cron
- WhatsApp 纳入 `_HOME_TARGET_ENV_VARS`

### Cache
- Portal Qwen TTL 从 1h 降至 5m（阿里上游限制）

### 其他
- `retry`: 浮点型 Retry-After 处理
- `send_message`: XMPP JID 识别
- `daytona`: 迁移到 cursor-based sandbox list
- Dashboard: 配置路径显示优化
- CI: e2e 超时延长至 15 分钟

---

## 新增贡献者

- JamesX88、NorethSea、laoli-no1、aqilaziz、AllynSheep、hookinglau、ryptotalent、zccyman、Osraka、AhmetArif0
